import { TimelineDatePipePipe } from './timeline-date-pipe.pipe';

describe('TimelineDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimelineDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
